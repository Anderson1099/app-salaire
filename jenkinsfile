node{
    stage('Clone git') {
        checkout scm
    }
    stage('ansible') {
      Playbook (
          colorized: true,          
          playbook: 'playbook.yml',
          inventory: 'hosts.yml'
      )
    }
}
